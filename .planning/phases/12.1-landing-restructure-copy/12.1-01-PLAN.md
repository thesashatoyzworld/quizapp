---
phase: 12.1-landing-restructure-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quiz-app/src/data/connectors.ts
autonomous: true

must_haves:
  truths:
    - "getConnectorsContent() returns preQualifier section with personalized text for all 5 archetypes"
    - "getConnectorsContent() returns beliefShift section with base text about collaborative work format"
    - "getConnectorsContent() returns forYouIf section with 5-7 self-selection criteria"
    - "Existing section copy updated to reflect weekly calls format, individual approach, and concrete 12-week results"
    - "Base content has all 3 new sections with generic (non-archetype) text"
    - "Deep merge handles new sections correctly without breaking existing overrides"
  artifacts:
    - path: "quiz-app/src/data/connectors.ts"
      provides: "3 new content sections (preQualifier, beliefShift, forYouIf) + updated copy"
      contains: "preQualifier"
  key_links:
    - from: "quiz-app/src/data/connectors.ts"
      to: "getConnectorsContent()"
      via: "deep merge of base + archetype overrides"
      pattern: "preQualifier.*beliefShift.*forYouIf"
---

<objective>
Add 3 new content sections and update existing copy in connectors.ts data file.

Purpose: Phase 12.1 restructures the landing page for research-backed conversion. This plan adds the data layer: 3 new sections (pre-qualifier, belief shift, "for you if...") and updates existing section copy to reflect the actual program format (weekly calls, individual approach, concrete 12-week results).

Output: Updated connectors.ts with 15 content sections (12 existing + 3 new), all correctly handled by deep merge and archetype overrides.
</objective>

<execution_context>
@C:/Users/OTVAJE/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/OTVAJE/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@quiz-app/src/data/connectors.ts
@.planning/research-sales-page-structure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 new content sections to baseContent and ContentOverrides type</name>
  <files>quiz-app/src/data/connectors.ts</files>
  <action>
Add 3 new sections to `baseContent` object in connectors.ts:

**1. `preQualifier` section** (goes before hero in page rendering):
```typescript
preQualifier: {
  text: 'Для экспертов, которые хотят выстроить систему привлечения клиентов и выйти на стабильный доход со своих знаний',
},
```

**2. `beliefShift` section** (goes after problem/3-elements):
```typescript
beliefShift: {
  title: 'Дело не в вас',
  subtitle: 'Дело в отсутствии системы',
  text: 'Вы не «плохой эксперт» и не «не умеете продавать». У вас просто нет работающей системы: продукт + контент + продажи, связанные в одну механику. Когда система есть — результат перестаёт зависеть от удачи, настроения и количества подписчиков.',
  shift: 'Это не курс — это совместная работа над вашим проектом в групповом формате',
},
```

**3. `forYouIf` section** (goes before final CTA):
```typescript
forYouIf: {
  title: 'Эта программа для вас, если',
  items: [
    'Вы эксперт с реальными навыками, но клиенты приходят хаотично или по сарафану',
    'Вы готовы выделять 5-7 часов в неделю на внедрение системы',
    'Вам нужна не теория, а совместная работа: разбор вашего проекта, обратная связь, конкретные шаги',
    'Вы хотите стабильный поток заявок из контента, а не разовые всплески',
    'Вы готовы действовать, а не просто слушать',
    'Вам важен результат за 12 недель: работающие продукты, воронка, контент-система',
  ],
},
```

Update `ConnectorsContent` type (it's inferred from `baseContent` via `typeof`, so adding to baseContent auto-updates the type).

Update `ContentOverrides` type to include optional overrides for all 3 new sections:
```typescript
preQualifier?: {
  text?: string;
};
beliefShift?: {
  title?: string;
  subtitle?: string;
  text?: string;
  shift?: string;
};
forYouIf?: {
  title?: string;
  items?: string[];
};
```

Update the `deepMerge` function to handle the 3 new sections:
- `preQualifier`: simple spread merge (like `important`)
- `beliefShift`: simple spread merge
- `forYouIf`: spread merge for title, replace items array if overrides.forYouIf.items exists (like weeks4to8)
  </action>
  <verify>
TypeScript compilation: `cd quiz-app && npx tsc --noEmit` should pass with no errors.
Verify new sections exist: grep for `preQualifier`, `beliefShift`, `forYouIf` in connectors.ts.
  </verify>
  <done>
baseContent has all 3 new sections. ContentOverrides type supports optional overrides for all 3. deepMerge handles all 3 new sections correctly. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add archetype-specific overrides for preQualifier (5 versions) and update existing copy</name>
  <files>quiz-app/src/data/connectors.ts</files>
  <action>
**Part A: Add preQualifier overrides for all 5 archetypes.**

Each archetype's `archetypeOverrides` entry gets a `preQualifier` field. These are personalized "Для экспертов, которые..." lines:

- **invisible**: `{ text: 'Для экспертов, которые сильны в своём деле, но которых никто не видит онлайн' }`
- **doer**: `{ text: 'Для экспертов, которые много учатся и делают, но никак не могут собрать всё в работающую систему' }`
- **generous**: `{ text: 'Для экспертов, которые щедро делятся знаниями, но зарабатывают меньше, чем заслуживают' }`
- **unstable**: `{ text: 'Для экспертов, у которых то густо, то пусто — и непонятно, от чего зависит результат' }`
- **scale**: `{ text: 'Для экспертов, которые упёрлись в потолок дохода и хотят масштабировать без выгорания' }`

**Part B: Update existing copy in `baseContent` to reflect program format.**

Update these sections in `baseContent` to reflect user's answers about the program format:

1. **`threeElements.title`**: Change from `'3 элемента, которые отделяют вас от 500К/мес'` to `'3 элемента системы'` (shorter, less hype per "прозрачность > пулепробиваемость" decision).

2. **`firstDays`**: Update items to reflect weekly calls format:
   - Item 1: `'Созвон 1-на-1 — разбираем вашу ситуацию, находим точку роста и составляем индивидуальный план'`
   - Item 2: `'Создаём ваш первый продукт — не шаблон, а именно ваш продукт под вашу нишу и аудиторию'`
   - Item 3: `'Первые клиенты — запускаем продажи и получаем обратную связь от рынка'`

3. **`author.description`**: Update to include weekly calls and individual approach:
   Change to: `'Создатель методологии «Коннектор» и мастер-класса «Продающий контент». Веду еженедельные созвоны, разбираю проект каждого участника лично. Не больше 10 человек — чтобы хватало внимания на каждого.'`

4. **`author.facts`**: Update to reflect format:
   - `'Еженедельные живые созвоны — разбираем текущие задачи, решаем проблемы в реальном времени'`
   - `'Смотрю контент каждого участника, даю индивидуальные рекомендации'`
   - `'Веду программу сама — без кураторов и помощников'`

5. **`week12.subtitle`**: Update to: `'К концу 12 недель совместной работы у вас есть:'`

6. **`week12.results`**: Update labels to be more concrete per user's answers:
   - `{ metric: 'Работающие', label: 'продукты, которые покупают' }`
   - `{ metric: 'Воронка', label: 'от контента до продажи' }`
   - `{ metric: 'Система', label: 'создания контента' }`
   - `{ metric: 'Скрипты', label: 'и лид-магниты' }`

Do NOT add beliefShift or forYouIf overrides per archetype — they stay universal (base content only) for now. The beliefShift message "Это не курс — совместная работа" is the same for all archetypes per locked decision.

IMPORTANT: Do NOT change any archetype-specific hero, threeElements items, firstDays items, whales items, weeks4to8 items, important, or cta overrides — those are already personalized from Phase 12 and must stay as-is.
  </action>
  <verify>
TypeScript compilation: `cd quiz-app && npx tsc --noEmit` should pass.
Verify all 5 archetypes have preQualifier override: grep for `preQualifier` in each archetype block.
Verify base content updates: grep for "еженедельные" or "Еженедельные" should find hits in author section.
Verify base content updates: grep for "совместной работы" should find hit in week12.subtitle.
  </verify>
  <done>
All 5 archetypes have personalized preQualifier text. Base content copy updated to reflect weekly calls, individual approach, and concrete 12-week results. No existing archetype overrides modified. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. `cd quiz-app && npx tsc --noEmit` — TypeScript compiles without errors
2. `getConnectorsContent()` (no param) returns object with preQualifier, beliefShift, forYouIf sections
3. `getConnectorsContent('invisible')` returns personalized preQualifier.text
4. `getConnectorsContent('doer')` returns personalized preQualifier.text
5. All 5 archetype overrides include preQualifier
6. Base content reflects program format: weekly calls, individual approach, 12-week results
7. Existing archetype overrides (hero, threeElements, firstDays, whales, weeks4to8, important, cta) unchanged
</verification>

<success_criteria>
- connectors.ts exports 15 content sections (12 existing + 3 new)
- All 5 archetypes have personalized preQualifier text
- beliefShift and forYouIf have base-only content (universal across archetypes)
- Copy reflects program format: weekly calls, individual approach, concrete 12-week deliverables
- TypeScript compiles without errors
- getConnectorsContent() deep merge handles all new sections
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-landing-restructure-copy/12.1-01-SUMMARY.md`
</output>
